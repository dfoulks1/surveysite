# Jekyll based survey website

## Getting started

ensure that ruby `bundler` and jekyll are installed

`gem install bundle`
`gem install jekyll`

Building the site

`bundle`            // install the prerequisites
`jekyll serve`      // build and serve the jekyll site locally
